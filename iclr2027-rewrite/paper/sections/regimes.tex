\section{Collective regimes}
\label{sec:regimes}

\num{This section reports the replicated $(d,\fd)$ map: the regime classification
with boundary uncertainty, the three correlations with their standard errors, the
test of the parameter-free prediction \eqref{eq:prediction} and the amplification
residual, the magnitude-matched controls, and the finite-size behaviour of the
ideological-alignment onset. It awaits the main campaign, which is in progress.}
